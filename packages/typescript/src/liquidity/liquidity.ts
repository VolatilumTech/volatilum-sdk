/**
 * Liquidity module.
 *
 * Purpose: types and utilities for interpreting liquidity topology and exposure.
 * This SDK does not compute PnL, returns, or projections.
 */
export type LiquidityExposure = {
  readonly kind: "unknown";
};
